# requirements.txt — specific to the OpenAI-first Streamlit + LangChain + FAISS RAG code you’re running

# --- Core UI ---
streamlit==1.37.1
rich==13.9.4                 # streamlit 1.37 requires rich<14
pandas==2.2.2
pillow==10.4.0

# --- LangChain + OpenAI (required for: from langchain_openai import ChatOpenAI, OpenAIEmbeddings) ---
langchain==0.2.15
langchain-core==0.2.37
langchain-community==0.2.15
langchain-text-splitters==0.2.2
langchain-openai==0.1.23
openai>=1.40.0,<2.0

# --- Vector store used in code (FAISS) ---
faiss-cpu>=1.8.0.post1; platform_system != "Windows"
# If deploying on Windows, uncomment:
# faiss-cpu-windows>=1.8.0.post1; platform_system == "Windows"

# --- Document loaders used in code ---
pypdf==4.2.0                 # PyPDFLoader
beautifulsoup4==4.12.3       # BSHTMLLoader
lxml==5.2.1                  # HTML parser backend
docx2txt==0.8                # Docx2txtLoader
python-pptx==0.6.23          # PPTX parsing dependency
unstructured>=0.14.0         # UnstructuredPowerPointLoader

# Excel reader for .xlsx (required by pandas.read_excel)
openpyxl==3.1.5

